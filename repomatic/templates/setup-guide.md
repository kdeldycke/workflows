---
args: [missing_permissions_section, step_token, step_dependabot, immutable_releases_step, step_branch_ruleset, step_fork_pr_approval, step_pypi_trusted_publisher, step_pages_source, step_virustotal, step_verify, org_tip, repo_url]
---

\$missing_permissions_section

\$step_token

\$step_dependabot

\$immutable_releases_step

\$step_branch_ruleset

\$step_fork_pr_approval

\$step_pypi_trusted_publisher

\$step_pages_source

\$step_virustotal

\$step_verify

\$org_tip

This issue will close automatically once all setup steps are verified. Repository state and configuration are continuously checked and enforced by the [`lint-repo` job]($repo_url/actions/workflows/lint.yaml).
