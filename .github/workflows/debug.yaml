---
name: Debug
"on":
  workflow_call:
  push:
  pull_request:
  schedule:
    # Run daily to gather context variables available in scheduler.
    - cron: "*/5 * * * *"

jobs:

  print-context:
    runs-on: ubuntu-20.04
    steps:
      - uses: crazy-max/ghaction-dump-context@v1.2.0
