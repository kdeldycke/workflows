---
name: Debug
"on":
  workflow_call:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/debug.yaml

jobs:

  print-context:
    runs-on: ubuntu-24.04
    steps:
      - uses: crazy-max/ghaction-dump-context@v2.3.0