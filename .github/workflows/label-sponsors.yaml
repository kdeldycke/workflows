
---
name: Label sponsors
"on":
  workflow_call:
  pull_request:
    types:
      - opened
  issues:
    types:
      - opened

jobs:

  label-sponsors:
    name: Tag sponsors
    runs-on: ubuntu-20.04
    steps:
      - uses: JasonEtco/is-sponsor-label-action@v1.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          label: 💖 sponsors