
---
name: Label sponsors
"on":
  pull_request:
    types: [opened]
  issues:
    types: [opened]

jobs:

  label-sponsors:
    name: Tag sponsors
    runs-on: ubuntu-20.04
    steps:
      - uses: JasonEtco/is-sponsor-label-action@v1.1.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          label: 💖 sponsors