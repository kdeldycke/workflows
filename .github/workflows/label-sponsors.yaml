
---
name: Label sponsors
"on":
  pull_request:
    types:
      - opened
      - reopened
  issues:
    types:
      - opened
      - reopened

jobs:

  label-sponsors:
    name: Tag sponsors
    runs-on: ubuntu-20.04
    # XXX Always skip that workflow while we wait for a fix to be merged upsteam:
    #   https://github.com/JasonEtco/is-sponsor-label-action/pull/30
    #   https://github.com/JasonEtco/is-sponsor-label-action/issues/28
    if: ${{ true }}
    steps:
      - uses: JasonEtco/is-sponsor-label-action@v1.1.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          label: 💖 sponsors